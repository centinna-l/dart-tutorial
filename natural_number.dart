void main() {
  int sum = 0;
  for (int i = 0; i <= 10; i++) {
    sum = sum + i;
  }
  print("The sum of first 10 natural numbers is: $sum");
}

// To make a variable to store the sum
// run a loop of first 10 natural numbers
// add the value of I to the variable sum to get the total of first 10 natural numbers.
